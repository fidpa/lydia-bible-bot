# dependencies (bun install)
node_modules

# output
out
dist
*.tgz

# exported chat transcripts (generated; may contain personal chat content)
exports/

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# user-specific MCP config (use mcp-config.example.ts as template)
mcp-config.ts

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Cursor
.cursor

# Finder (MacOS) folder config
.DS_Store
/claude-bot-standalone

# Symlink to shared-hot exchange directory
/shared

# Whisper model files (large binaries)
models/

# Leftover local database of the removed embedded Bible server. The rule stays
# so an old working copy cannot be committed by accident; Bible text now comes
# from the hosted endpoint and is never stored here.
bible_mcp/data/

# Local Claude Code workspace (slash commands, local settings). Never published.
# CLAUDE.md is deliberately NOT listed here: it is the bot's system prompt, a
# tracked and documented part of the product (see README "Architecture").
.claude/

# Personal/marketing
LINKEDIN_POST.md
REDDIT_POST.md

# explain-audio output (lokal generiertes TTS-Hörstück, .txt + .mp3)
/explain-audio/
